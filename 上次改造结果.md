真实检索链路替换完成
BM25：rank-bm25 (BM25Okapi)
向量检索：Sentence-Transformers embedding + Chroma 持久化向量库（Chroma异常时自动回退内存向量检索）
重排：Cross-Encoder（加载失败自动回退启发式重排）
核心实现：rag_engine.py
真实 LLM/Embedding Provider 打通
llm 参数已从 API 请求贯通到生成链路（provider/model/temperature/base_url/api_key）
接入 OpenAI-compatible 调用逻辑，DeepSeek 可直接用
mock 仍保留为降级方案
路由与模型改动：chat.py, schemas.py
多轮上下文窗口已补齐
chat 会读取会话历史窗口
在代词/短问场景下将上一轮用户问题并入检索 query
生成时注入历史消息窗口（user/assistant）
实现位置：rag_engine.py
配置已写入

已新增：backend/.env
包含 DeepSeek base_url、api_key、默认模型、向量库与重排模型配置。
联调相关改动

索引接口已传入 bm25/vector 开关和 embedding model：docs.py
检索调试接口关闭生成，纯看检索链路：retrieve.py
前端 legalRag 默认 llm 改为 deepseek-chat，索引默认 embedding 模型改为 sentence-transformers：legalRag.ts
自检结果

后端导入成功：backend_import_ok
检索自检成功：返回 vector/rerank 结果
mock 问答成功：返回 session_id + citations
DeepSeek 真实问答成功：返回有效中文答案 + 引用
说明

第一次运行会下载 embedding 与 cross-encoder 模型，启动和首问会慢，这是正常现象。
你现在可以直接启动后端/前端，走 legal-shell 完整联调链路。